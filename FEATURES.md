# Auto-App - Система управления автосервисом

## 🆕 Новые функции (версия 2.0)

### 1. Модель услуг (Services)
- Добавление, редактирование, удаление услуг
- Каждая услуга содержит: название и цену
- Данные сохраняются в `localStorage`

**Путь**: Администратор → Вкладка "Услуги"

### 2. Управление пользователями (Users)
Система поддерживает 3 роли:

#### 👤 Клиент (Client)
- Базовая роль для клиентов сервиса
- Может быть привязан к заказам

#### 👨‍🔧 Мастер (Master)
- Выполняет услуги
- Каждый мастер может иметь несколько специализаций
- При создании мастера выбираются его услуги (checkbox)

#### 👨‍💼 Оператор (Operator)
- Управляет сменами
- Может открывать и закрывать смены
- Информация о текущей смене отображается в списке пользователей

**Путь**: Администратор → Вкладка "Пользователи"

### 3. Система смен (Shifts)
Операторы могут открывать и закрывать смены для контроля рабочего времени.

#### Открытие смены
1. Перейти в Администратор → Вкладка "Смены"
2. Выбрать оператора
3. Нажать "🟢 Открыть смену"

#### Закрытие смены
1. Нажать "🔴 Закрыть смену"
2. Добавить примечание (опционально)
3. Подтвердить закрытие

#### История смен
- Отображает все открытые и закрытые смены
- Показывает длительность смены
- Содержит информацию об операторе

**Путь**: Администратор → Вкладка "Смены" (по умолчанию)

---

## 📁 Структура кода

### `/src/store/dataStore.js`
Центральное хранилище для всех операций с данными:
- `getServices()`, `addService()`, `updateService()`, `deleteService()`
- `getUsers()`, `addUser()`, `updateUser()`, `deleteUser()`
- `getOperators()`, `getMasters()`, `getClients()`
- `openShift()`, `closeShift()`, `getCurrentShift()`, `getShiftsByOperator()`

### `/src/components/AdminPanel.js`
Главная админ-панель с вкладками для управления всем функционалом.

### `/src/components/ServicesManagement.js`
Компонент для управления услугами.

### `/src/components/UsersManagement.js`
Компонент для управления пользователями всех ролей.

### `/src/components/ShiftsManagement.js`
Компонент для управления сменами операторов.

---

## 💾 Сохранение данных

Все данные сохраняются в `localStorage`:
- `services` - список услуг
- `users` - список пользователей
- `shifts` - история смен

---

## 🎯 Примеры использования

### Добавить новую услугу
```
1. Администратор → Услуги
2. Нажать "+ Добавить услугу"
3. Ввести название (например: "Замена масла")
4. Ввести цену (например: 1000)
5. Нажать "Добавить"
```

### Добавить мастера с услугами
```
1. Администратор → Пользователи
2. Нажать "+ Добавить пользователя"
3. Ввести имя (например: "Иван Петров")
4. Выбрать роль "Мастер"
5. Отметить услуги, которые он может выполнять
6. Нажать "Добавить"
```

### Открыть смену оператора
```
1. Администратор → Смены (вкладка по умолчанию)
2. Найти оператора в списке
3. Нажать "🟢 Открыть смену"
```

---

## 🔄 Интеграция с записями

В будущем услуги и мастера могут быть привязаны к записям (заказам) в основном списке.
