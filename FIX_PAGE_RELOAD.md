# Исправление ошибки "Not found" при обновлении страницы

Вы видите "Not found" при перезагрузке на `/records` или `/admin`? Это означает, что сервер не настроен для SPA (Single Page Application).

## Что было сделано

✅ Добавлены файлы конфигурации:
- `.htaccess` (для Apache)
- `_redirects` (для Vercel/Netlify)

✅ Добавлен `"homepage": "."` в `package.json`

✅ Обновлен `App.js` с поддержкой `basename`

## Что нужно сделать ВЫ

### Шаг 1️⃣: Пересоберите приложение

```bash
npm run build
```

Это создаст папку `build/` с готовым приложением и скопирует все конфиги.

### Шаг 2️⃣: Определите тип хостинга

**Какой хостинг вы используете?**

- ✅ **Vercel** → перейдите на [Vercel деплой](#vercel)
- ✅ **Netlify** → перейдите на [Netlify деплой](#netlify)
- ✅ **Apache** (Beget, Reg.ru, Hostinger и т.д.) → перейдите на [Apache инструкция](#apache)
- ✅ **Другой** → смотрите [HOSTING_SETUP.md](./HOSTING_SETUP.md)

### Шаг 3️⃣: Загрузите на хостинг

Загрузите **ВСЕ содержимое папки `build/`** на хостинг (обычно через FTP или панель управления).

⚠️ Важно: загружайте **содержимое** папки `build/`, а не саму папку `build/`!

### Шаг 4️⃣: Проверьте

1. Откройте сайт
2. Перейдите на `/records` или `/admin`
3. Обновите страницу (F5)
4. Должно работать! ✅

---

## Инструкции для конкретных хостингов

### Vercel

1. Установите Vercel CLI:
   ```bash
   npm install -g vercel
   ```

2. Запустите деплой:
   ```bash
   vercel
   ```

3. Следуйте инструкциям — все настроится автоматически! ✅

### Netlify

1. Установите Netlify CLI:
   ```bash
   npm install -g netlify-cli
   ```

2. Запустите деплой:
   ```bash
   netlify deploy --prod --dir=build
   ```

3. Следуйте инструкциям ✅

### Apache

**Если хостинг Beget, Reg.ru, Hostinger, 1C-Битрикс и т.д.:**

1. Пересоберите: `npm run build`
2. Через FTP или панель управления загрузите всё из папки `build/` на хостинг
3. **Убедитесь, что файл `.htaccess` загружен!** (он скрытый, включите отображение скрытых файлов)
4. Обновите страницу в браузере

**Если все ещё не работает:**

Обратитесь в поддержку хостера:
> "Пожалуйста, включите модуль mod_rewrite и разрешьте файлы .htaccess на моём хостинге. Мне нужно настроить SPA routing для React приложения."

---

## Диагностика (если ничего не помогло)

### Откройте консоль браузера (F12)

1. **Вкладка Console** — есть ошибки?
2. **Вкладка Network** → Обновите страницу на `/records`:
   - Есть строка `index.html` со статусом 200? ✅ Конфиг работает!
   - Есть строка `records` со статусом 404? ❌ Конфиг не работает

### Проверьте, что загружено на хостинг

1. Откройте панель управления хостингом
2. Проверьте, что в корне сайта есть:
   - `index.html` ✅
   - `.htaccess` ✅
   - Папка `static/` ✅

3. Если `.htaccess` отсутствует:
   - Включите отображение скрытых файлов в FTP-клиенте
   - Или создайте его вручную на хостинге с содержимым из проекта

---

## Если вы знаете, какой хостинг

**Напишите в комментариях или откройте issue с информацией:**
- Название хостинга
- Структура URL (например, `example.com` или `example.com/subfolder`)
- Какая ошибка в браузере (скрин консоли)

И я помогу более точной инструкцией! 🚀
